# GoldTubeHouse 字幕分段整理系統使用說明書 (User Manual)

## 1. 系統簡介
GoldTubeHouse 是一個專為 YouTube 影片字幕下載、智慧分段與 AI 重點筆記生成設計的自動化工具。它可以協助您將數小時的長影片自動拆分為好讀的語意區段，並透過 OpenAI 的強大能力一鍵生成重點摘要與專業中英詞彙對照表。

---

## 2. 核心功能操作步驟

### 第一步：貼上 YouTube 影片網址
1. 在首頁輸入框中貼上您想要處理的 YouTube 影片網址（例如：`https://www.youtube.com/watch?v=2GZ2SNXWK-c`）。
2. 點擊 **開始處理** 按鈕。系統將自動剖析影片資訊、抓取中/英字幕，並依照預設規則對字幕進行自動分割。

### 第二步：分段參數微調 (右側邊欄)
若您需要自訂分段邏輯，可在右側的「分段參數微調」卡片中進行設定：
1. **自訂分段區間 (預設 20 分鐘)**：當影片長度需要被切分時，以此時間長度作為每一段的基礎目標長度。下方提供 5分、10分、15分、20分、30分 的快捷按鈕。
2. **直接顯示門檻 (預設 30 分鐘)**：
   * 影片長度**低於 30 分鐘**時，系統會判定無需分割，直接將整部影片作為 1 個完整段落顯示。
   * 影片長度**大於等於 30 分鐘，且當達到 40 分鐘時**，系統會以自訂分段區間（20分鐘）為單位進行切分（例如 40 分鐘影片會被切分為兩個 20 分鐘區段）。
3. **超長章節細分門檻 (預設 30 分鐘)**：當單一章節（Chapter）的長度大於此門檻時，會以「自訂分段區間」進行二次細分。

### 第三步：手動調整與編輯 (Edit Mode)
* **手動 Enter 切分**：
  在主界面的「編輯與分割區塊」分頁中，您可以直接點擊任意字幕卡片中的文字。在任意句點後按下鍵盤的 **Enter 鍵**，即可在該時間戳記處精準將卡片切分為二，後半段字幕會自動後移。
  * *註 1：為確保段落意義完整，分割出的前半段長度必須大於 5 分鐘（300 秒），否則將觸發防誤觸提示（「分割出的前半段需大於 5 分鐘，已自動合併」）而不予分割。*
  * *註 2：系統已支援「句內單字邊界精準切分」。若您的游標剛好落在一行字幕的單字內部（例如在 `the back to clean that up. So the next` 中間按 Enter），系統會自動將該行字幕拆分為二，重新分配時間戳記，確保前半段 `the back to clean that up.` 留在上一區塊，而後半段 `So the next` 精準開啟新區塊，絕不會讓前面的文字污染新的卡片。*
  * *註 3：系統設有「尾部自動合併機制」。如果您切分後，該段落剩下的尾部時間太短（低於 6 分鐘，或小於自訂分段區間的一半），剩餘的尾部區段會自動合併到前一個區塊中，避免產生只剩 5 分鐘左右的極短區塊。*
* **字幕內容編輯**：您可以直接在卡片中修改或刪除錯別字。

### 第四步：鎖定並呼叫 AI 生成
1. 當您確認分段與編輯無誤後，點選右下角的 **🔒 鎖定分段並預估 API 費用**。
2. 系統會顯示預估的總文字字數、OpenAI gpt-5.1 的 API 輸入/輸出 Tokens 以及預估的美金/台幣費用。
3. 確認無誤後，點選 **🚀 確認呼叫 AI 開始整理**。
4. 整理完畢後，您可在左側切換至 **AI 整理筆記** 與 **專業術語對照** 分頁檢視並點擊複製。

---

## 3. 常見問題 (FAQ)
* **Q：為什麼我的 25 分鐘影片沒有被分段？**
  * A：因為系統的「直接顯示門檻」預設已調整為 30 分鐘。任何低於 30 分鐘的影片皆會被保留為單一完整區塊（只需一次即可）。如果您希望將它切分，可至右側邊欄將「直接顯示門檻」手動下調為更低的分鐘數（例如 10 分鐘）。
* **Q：40 分鐘影片的分割行為為何？**
  * A：40 分鐘影片大於 30 分鐘的直接顯示門檻，因此會啟用分段。系統以預設 20 分鐘的自訂分段區間為基準，將其自動切分為兩個 20 分鐘的段落（即 20 20 兩次）。
* **Q：如何匯入本地的 API Key？**
  * A：如果您在執行伺服器的系統環境變數中設定了 `OPENAI_API_KEY`，在前端的 OpenAI API 設定區塊會顯示「偵測到本地環境中有 API Key [點此引入]」，點擊即可自動讀取。
